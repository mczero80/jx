NEEDLIBS = zero jdk0 zero_misc
NEEDLIBS += jdk1

NEEDLIBS += bio
NEEDLIBS += bio_ram
NEEDLIBS += buffer

#NEEDLIBS += timer timer_pc

NEEDLIBS += fs_buffer
NEEDLIBS += fs
NEEDLIBS += fs_vfs
NEEDLIBS += fs_javafs
NEEDLIBS += collections # needed by fs_buffer_impl
NEEDLIBS += fs_buffer_impl
NEEDLIBS += fs_user

SUBDIRS = jx/fs

LIBNAME = fs_user_impl
CLASSFILES = jx/fs/*.class
NUMBERENV = int


